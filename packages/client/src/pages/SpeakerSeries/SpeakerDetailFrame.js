import React from 'react';
import SpeakerProfile from './SpeakerProfile';

// Shows more detailed speaker series information.
const SpeakerDetailFrame = ({ speaker }) => (<SpeakerProfile speaker={speaker}/>);

export default SpeakerDetailFrame;
