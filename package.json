{
  "name": "coved",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "lerna run start --stream",
    "build": "lerna run build --stream",
    "test": "lerna run test --stream",
    "preproduction": "npm run build",
    "production": "NODE_ENV=production node packages/server/bin/www"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "dependencies": {
    "redoc-cli": "^0.9.8"
  }
}
